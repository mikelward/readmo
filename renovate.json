{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommitsDisabled"
  ],
  "timezone": "UTC",
  "description": [
    "Renovate is eligible to run at any time. Update volume is paced by the per-update-type minimumReleaseAge cooldowns and prConcurrentLimit below, not by a weekly window — a window only delays PRs that are already cooled down.",
    "mode=full is stated explicitly because the Mend-hosted app defaults an 'All repositories' install to silent mode, in which Renovate opens no PRs and no dependency dashboard issue — indistinguishable from 'nothing needs updating'. If PRs still never appear, the remaining switch is Mend-side (developer.mend.io → repo/org → Interactive), not here.",
    "The npm constraint below is load-bearing, not decoration. The .npmrc min-release-age window is only enforced by the npm that does the resolving, and for lock file maintenance that npm is Renovate's, not ours. npm added the option in 11.10.0 and silently ignores it before that ('Unknown project config'), so without this constraint the one path the window exists to cover could still regenerate the lockfile with an npm that has never heard of it — the same silent no-op the rest of this config is built to avoid. Renovate also infers constraints from engines.npm; stating it here as well keeps it true if engines is ever relaxed."
  ],
  "mode": "full",
  "schedule": ["at any time"],
  "assignees": ["mikelward"],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 0,
  "rangeStrategy": "bump",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["at any time"]
  },
  "platformAutomerge": true,
  "constraints": {
    "npm": ">=11.10.0"
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "supabase/functions/import_map.json pins the EXACT npm versions the Edge runtime resolves at deploy time (there is no deno.lock, and `supabase functions deploy --import-map` re-resolves every deploy). It has no ecosystem behind it, so no built-in manager sees it: a bump would edit package.json + the lockfile and silently leave the map behind — which is how `entities` ended up at ^7.0.1 against package.json's ^8.0.0, running the tests on 8.x while production served 7.x. import_map.test.ts fails CI on that drift; this manager is what keeps the bump PR green in the first place, by updating the map in the SAME branch (Renovate branches per depName, and both managers report the same depName + version).",
      "managerFilePatterns": ["/^supabase/functions/import_map\\.json$/"],
      "matchStrings": ["\"npm:(?<depName>@?[^\"@]+(?:/[^\"@]+)?)@(?<currentValue>[^\"]+)\""],
      "datasourceTemplate": "npm",
      "versioningTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "description": "Cooldown before an update is offered (mirrors the old dependabot semver-aware cooldown).",
      "matchUpdateTypes": ["major"],
      "minimumReleaseAge": "14 days"
    },
    {
      "matchUpdateTypes": ["minor"],
      "minimumReleaseAge": "7 days"
    },
    {
      "matchUpdateTypes": ["patch", "pin", "digest"],
      "minimumReleaseAge": "5 days"
    },
    {
      "description": "Auto-merge every patch and minor once CI is green; majors always wait for review. Pre-1.0 (0.x) packages are excluded — SemVer permits breaking changes in a 0.x minor, so they still open a PR for review.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "matchCurrentVersion": "!/^0/",
      "automerge": true
    },
    {
      "description": "Lock file maintenance auto-merges too, but as its own rule: a lockFileMaintenance branch refreshes the whole lockfile and has no single dependency behind it, so it has no current version for matchCurrentVersion to test. Folded into the rule above it would silently never match, and those PRs would pile up unmerged.",
      "matchUpdateTypes": ["lockFileMaintenance"],
      "automerge": true
    },
    {
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Grouping (mirrors the old dependabot groups; keeps peer-related packages in one PR).",
      "groupName": "tanstack",
      "matchPackageNames": ["@tanstack/**"]
    },
    {
      "groupName": "types",
      "matchPackageNames": ["@types/**"]
    },
    {
      "description": "@types/node tracks the Node major we actually run (.nvmrc / engines.node), so a major bump here is a runtime migration, not a dependency update — nodeVersion.test.ts fails CI on the mismatch. Renovate would otherwise re-offer it every week for a PR that can't be merged on its own.",
      "matchPackageNames": ["@types/node"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "The Node runtime is pinned by MAJOR on purpose: `.nvmrc` holds the bare `24` so CI, `nvm use`, and the session-start hook each resolve the newest release of that major on their own, and nodeVersion.test.ts asserts that exact string. Renovate's nvm manager can only write a full version, so every 'Update Node.js to v24.x.y' PR rewrites .nvmrc to `24.18.1`, fails CI, and reopens the next time a patch ships — three repos' worth of unmergeable PRs for an upgrade that already happens at runtime without a commit. Patches/minors are therefore off entirely.",
      "matchDepNames": ["node"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "enabled": false
    },
    {
      "description": "A Node MAJOR is a runtime migration, not a dependency update — .nvmrc, engines.node and @types/node move in one commit (see CLAUDE.md 'Node version'). Renovate can't do that, so it must not open the PR unprompted; dependencyDashboardApproval keeps the new major visible on the dashboard, and checking that box is the deliberate 'I am doing the migration now' signal. Expect to fix .nvmrc back to the bare major by hand in that branch.",
      "matchDepNames": ["node"],
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "TypeScript 7 replaced the compiler with the native port and changed the public API surface; typescript-eslint refuses to load against it outright ('typescript-eslint does not support TS 7.0'), so `npm run lint` dies before it lints anything. Taking it means running two TypeScript installs side by side — a toolchain migration, not a dependency update. Tracked upstream as typescript-eslint#10940; re-enable once that lands and do the move deliberately, all at once.",
      "matchPackageNames": ["typescript"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "groupName": "eslint",
      "matchPackageNames": [
        "eslint",
        "eslint-**",
        "@eslint/**",
        "@eslint-community/**",
        "@typescript-eslint/**",
        "typescript-eslint"
      ]
    },
    {
      "groupName": "testing",
      "matchPackageNames": [
        "@testing-library/**",
        "vitest",
        "@vitest/**",
        "jsdom",
        "msw",
        "fake-indexeddb"
      ]
    },
    {
      "groupName": "vite",
      "matchPackageNames": ["vite", "@vitejs/**", "vite-plugin-**"]
    },
    {
      "groupName": "react",
      "matchPackageNames": ["react", "react-dom", "react-router-dom"]
    },
    {
      "groupName": "supabase",
      "matchPackageNames": ["@supabase/**"]
    },
    {
      "groupName": "workbox",
      "matchPackageNames": ["workbox-**"]
    },
    {
      "description": "Both the scoped families we use — @fontsource/* and @fontsource-variable/*. The glob has to reach across the scope separator, which `@fontsource**` does not: minimatch treats `**` as a cross-directory globstar only when it is a whole path segment, so an embedded one behaves like `*` and stops at the `/`. That spelling matched nothing at all, and the group silently never fired.",
      "groupName": "fonts",
      "matchPackageNames": ["@fontsource*/**"]
    },
    {
      "description": "GitHub Actions get their own group. No minimumReleaseAge here on purpose: the per-update-type cooldowns above already apply to every manager, and repeating one here would override the 14-day major cooldown with 7 days for actions majors (later rules win on scalar fields).",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    }
  ]
}
