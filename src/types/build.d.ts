// Injected by Vite's `define` at config-load time (see vite.config.ts).
// The ISO commit timestamp of the deployed bundle, or '' when git metadata
// is unavailable (shallow checkout, tarball). The UI hides the row when ''.
declare const __BUILD_COMMIT_TIME__: string;
